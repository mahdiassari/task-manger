import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    allVariants: {
      color: "#212121",
    },
    button: {
      textTransform: "none",
    },
  },
});

export default theme;
